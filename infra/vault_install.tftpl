#cloud-config
package_update: true
packages:
  - unzip
runcmd:
  - 'wget --no-verbose https://releases.hashicorp.com/vault/${vault_version}/vault_${vault_version}_linux_amd64.zip -P /tmp'
  - 'unzip /tmp/vault_${vault_version}_linux_amd64.zip -d /usr/local/bin'