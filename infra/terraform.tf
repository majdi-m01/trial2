terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
    random = {
      source  = "hashicorp/random"
      version = "~> 3.1"
    }
  }
  # NOTE: No backend block is configured here.
  # Terraform will use a local state file (terraform.tfstate) by default.
}
